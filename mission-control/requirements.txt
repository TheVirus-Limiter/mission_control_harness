pydantic>=2.6
pyyaml>=6.0
rich>=13.7
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27
anthropic>=0.40
openai>=1.40
pytest>=8.0
# Only needed for REAL posting to X (OAuth1 signing). Dry-run never imports it.
requests>=2.31
requests-oauthlib>=1.3
